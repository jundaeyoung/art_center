<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bclass.arts_center.repository.interfaces.PaymentRepository">

	<select id="selectAdminKey" resultType="com.bclass.arts_center.dto.payment.AdminKeyDto">
		SELECT admin_key
		FROM payment_adminkey_tb
		WHERE id = 1
	</select>
	
	<insert id="insertPayment">
		INSERT INTO payment_tb(payment_id, user_id, payment_option, payment_date, ticketing1_id)
		VALUES (#{paymentId},#{userId}, #{paymentOption}, #{paymentDate}, #{ticketingId});
	</insert>
	
	<!-- 작성자 : 편용림 대관 결제 -->
	<insert id="insertManagerPayment">
		INSERT INTO manager_payment_tb(m_payment_id, manager_id, payment_option, payment_date, rent_id)
		VALUES (#{mPaymentId},#{managerId}, #{paymentOption}, #{paymentDate}, #{rentId});
	</insert>


</mapper>